name: Example

on:
  push:
    branches:
      - '*'
    # paths:
    #   - 'src/**.ts'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./.github/workflows/base-test.yml
